name: Run unit tests on local Go environment

on:
    push:
      branches:
        - dev
      paths-ignore:
        - '**.md'
    pull_request:
      branches: 
        - '*'
      paths-ignore:
        - '**.md'
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-go@v5
        with:
          go-version: 'stable'
          check-latest: true
      - uses: actions/checkout@v4
      - name: Run tests
        run: |
            go test ./... -race -timeout=10s
